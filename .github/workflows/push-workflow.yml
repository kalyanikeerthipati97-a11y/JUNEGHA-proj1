name: Push_workflow
on:
  push:
    branches:
      - main
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: print Greetings
        run: echo "Hello, World!"
      - name: print GoodBye
        run: |
          echo "Hello, World!"
          echo "workflow executed successfully!"      